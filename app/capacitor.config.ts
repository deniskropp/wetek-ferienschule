import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.wetek',
  appName: 'ferienschule-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
