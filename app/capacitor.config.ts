import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.wetek',
  appName: 'Ferienschule',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
